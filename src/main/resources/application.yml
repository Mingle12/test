server:
  port: 8080

spring:
  profiles:
    active: prod

---
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&rewriteBatchedStatements=true&allowPublicKeyRetrieval=true
    username: root
    password: root
  profiles: dev
---
spring:
  datasource:
    platform: h2
    url: jdbc:h2:~/test1
    username: sa
    password: sa
    driverClassName: org.h2.Driver
  profiles: prod